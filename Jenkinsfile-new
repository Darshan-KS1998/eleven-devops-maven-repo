pipeline {
    // add your slave label name
    agent { label 'node1'}
    tools{
        maven 'maven-3.8.8'
    }
    stages {
        stage ('Checkout_SCM') {

            steps {
          	    
	     checkout scm
            }
        }

        stage ('Maven_Build') {

            steps {
               sh 'mvn clean package'
            }
        }
        
        stage ('Deploy_Tomcat') {

            steps {
               sshagent(['ssh-key']) {
              sh "scp -o StrictHostKeyChecking=no  target/maven-web-application.war  ec2-user@51.20.140.166:/opt/tomcat9/webapps"
            
                    
            }
         }
        }
        
    }
}
